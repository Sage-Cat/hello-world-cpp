# Hello World in C++

A minimal C++ program that writes `Hello, World!` to standard output.

## Build and run

```sh
g++ -std=c++17 -Wall -Wextra -Wpedantic -Werror hello.cpp -o hello
./hello
```

Expected output:

```text
Hello, World!
```
